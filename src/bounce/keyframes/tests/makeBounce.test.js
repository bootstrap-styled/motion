import makeBounce from '../makeBounce';

describe('makeBounce', () => {
  describe('makeBounce', () => {
    it('should returns an object', () => {
      expect(typeof makeBounce()).toBe('object');
    });
  });
});
