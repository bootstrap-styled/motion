/**
 * Testing our styled Special utils
 */

import makeFlash from '../makeFlash';

describe('makeFlash', () => {
  describe('makeFlash', () => {
    it('should returns an object', () => {
      expect(typeof makeFlash()).toBe('object');
    });
  });
});
