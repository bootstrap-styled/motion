const main = {
  '$motion-duration': 'Duration',
  '$motion-delay': 'Delay',
  '$motion-timing-function': 'Timing',
};

const animation = {
  '$motion-direction': 'Direction',
  '$motion-play-state': 'Play State',
  '$motion-iterations': 'Iterations',
  '$motion-fill-mode': 'Fill Mode',
};

const settings = {
  main,
  animation,
};

export default settings;

