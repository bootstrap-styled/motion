/**
 * Testing our ControlPanelForm export
 */
import ControlPanelForm from '../index';

describe('ControlPanelForm', () => {
  it('should contains valid export', () => {
    expect(ControlPanelForm).toBeDefined();
  });
});
